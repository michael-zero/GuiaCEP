import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import Screen from '../../components/Screen'

const index = () => {
    return (
        <Screen>
            <Text>Aqui serão listados os favoritados</Text>
        </Screen>
    )
}

export default index

const styles = StyleSheet.create({})
